[Unit]
Wants=network-online.target researka-fullraw-search.service
After=network-online.target researka-fullraw-search.service

[Service]
# EnvironmentFile values override Environment= regardless of directive order.
# Load the canonical shared route last so stale legacy route files cannot win.
EnvironmentFile=/etc/v5-memo/portfolio-shared-fullraw.env
