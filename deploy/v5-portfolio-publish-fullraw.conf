[Unit]
# Add V5-owned bounded capacity without replacing the platform-owned shared
# dependency or its final EnvironmentFile contract.
Wants=v5-memo-publish-fullraw-search.service
After=v5-memo-publish-fullraw-search.service

[Service]
Environment=V5_MEMO_PORTFOLIO_FULL_RAW_CORPUS_SEARCH_URL=http://127.0.0.1:9935/search
